# ── Secrets — NEVER commit ───────────────────────────────────────────────────
.env
**/.env
!.env.example
!**/.env.example

# ── Local agent / tooling config (not for public repo) ───────────────────────
# Unanchored so they match at any depth (root *and* dr-votia/, dr-votia-web/, …).
.claude/
.agents/
.atl/
skills-lock.json
/.mcp.json
/images/

# ── Design exports (large, regenerable) ──────────────────────────────────────
stitch_custom_web_design_project/
stitch_custom_web_design_project.zip

# ── Node / Next.js ───────────────────────────────────────────────────────────
node_modules/
.next/
out/
build/
*.tsbuildinfo
next-env.d.ts

# ── Python ───────────────────────────────────────────────────────────────────
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.mypy_cache/
.ruff_cache/
.pytest_cache/

# ── OS ───────────────────────────────────────────────────────────────────────
.DS_Store
**/.DS_Store
